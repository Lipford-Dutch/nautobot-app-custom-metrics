Aligned the CI test matrix and Docker/Invoke defaults to the supported Nautobot floor (`3.1.0`). Previously CI built against Nautobot `3.0.0`, which lacks the `extras` migration the app's initial migration depends on, causing `check-in-docker` and `unittest` to fail with `NodeNotFoundError`.
